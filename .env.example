# Copy to .env and adjust for local Spring Boot.
VITE_API_URL=http://localhost:8080/api/v1

# true = localStorage/mock. false = Spring Boot + JWT (login/register via /auth/*).
VITE_USE_MOCK=false

# MySQL seed (UTF-8): mysql -u root --default-character-set=utf8mb4 pawshope < scripts/seed-utf8.sql
