# Copy to .env and adjust for local Spring Boot (default port 8080).
VITE_API_URL=http://localhost:8080/api/v1

# true = localStorage/mock (default). false = call Spring Boot for shop products.
VITE_USE_MOCK=false
